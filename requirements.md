## Packages
framer-motion | For premium page transitions and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes safely

## Notes
- Using Replit Auth for user authentication (/api/login, /api/logout)
- Cart state managed via React Context (in-memory)
- Assuming @shared/routes exports the defined API contract
- Assuming @shared/schema exports the Drizzle types
